version: '3.8'
services:
  db:
    image: postgres:latest
    restart: always
    environment:
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: postgres
      POSTGRES_USER: admin
    volumes:
      - ./data:/var/lib/postgresql/data
    ports:
      - "5432:5432"